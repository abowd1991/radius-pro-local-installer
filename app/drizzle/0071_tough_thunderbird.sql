-- Migration 71: radacct index cleanup
-- All target indexes already exist in production DB (applied manually during VPS audit)
-- This migration is a no-op to keep the migration history consistent
SELECT 1;
